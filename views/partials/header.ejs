<!doctype html>
<html lang="en">
	<head>
		<title>MoviesCorner</title>
		<link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
		<link href='https://fonts.googleapis.com/css?family=Miltonian' rel='stylesheet'>
  </head>
  <body>
    <header class="icon">
			<div class="headlogin">
  <!--    <p>Not signed in</p> -->
	<% if(data==undefined || data.user==null){%>
				Not Logged In

        <%}else{%>
    				<%= data.user.firstName+' '+ data.user.lastName%>
    		<% } %>

			<%#}else{
          if(data.user.firstName==undefined){%>
					       <%#= data.user._firstName+' '+data.user._lastName%>
          <%# }else{ %>
                <%#= data.user.firstName+' '+data.user.lastName%>
			<%# } }%>
		</div>
      <h1><img class="img1" src="/images/2-1.jpg" alt="logo">
          MOVIES CORNER
      </h1>
    </header>
  </body>
</html>
